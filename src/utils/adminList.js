// List of email addresses authorized for Admin Access
export const ADMIN_EMAILS = [
    'admin@nacoss.com',
    'elections@nacoss.com',
    'kingsblanc@gmail.com'
].map(email => email.toLowerCase()); // Ensure all are lowercase for comparison
